import SwiftUI

extension Color {
    static let primaryTeal = Color(red: 47/255, green: 176/255, blue: 199/255) // #2fb0c7
    static let backgroundLight = Color.white
    static let textPrimary = Color.black
    static let textSecondary = Color.gray
    // Add other colors as needed based on UIUX documents
}
